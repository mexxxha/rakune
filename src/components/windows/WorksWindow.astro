---
import Window from '../Window.astro';
import { artworkAlt } from '../../lib/seo';
import type { Work } from '../../lib/types';

interface Props {
  works: Work[];
}

const { works } = Astro.props;
---

<Window id="works" title="作品" extraClass="window--works">
  <div class="explorer">
    <div class="explorer__toolbar">作品フォルダ</div>
    <div class="explorer__list">
      {
        works.map((work) => (
          <button class="work-icon" type="button" data-work-id={work.id} aria-label={work.title}>
            <img src={work.thumb} alt={artworkAlt(work)} width="112" height="112" loading="lazy" />
            <span aria-hidden="true">{work.title}</span>
          </button>
        ))
      }
    </div>
    <div class="explorer__status">{works.length} 個のオブジェクト</div>
  </div>
</Window>
